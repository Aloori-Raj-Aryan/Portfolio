# AI Image Tracking

A prototype that uses computer vision assets stored in the project folder to display project details automatically on the page.

This project demonstrates how the site can load project data from `assets/projects` and update content without editing `index.html` directly.
